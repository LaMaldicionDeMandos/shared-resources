<div class="container">
    <div class="block-header" ng-controller="profileController">
        <h2>{{profile.username}} <small ng-if="profile.fullName">{{profile.fullName}}</small></h2>
    </div>
</div>