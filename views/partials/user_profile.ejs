<div class="container">
    <div class="block-header" ng-controller="profileController">
        <h2>{{user.username}} <small ng-if="user.profile.fullName">{{user.profile.fullName}}</small></h2>
    </div>
</div>